# CodeCrew 1.0 announcements — 2026-08-27

Posted by wordy from the RadiusRed accounts after the operator resolved the
gate on [www#47](https://github.com/radiusred/www/issues/47). Links go to the
project home first, the article second (operator instruction on the issue).

## Bluesky — `radiusred.bsky.social`

Text (`[label](url)` becomes display text with a link facet; `#tags` are
faceted; the link card is the project home):

```
CodeCrew 1.0 is out: agent-driven software delivery on plain GitHub — issues, PRs, four role contracts, and a CLI that refuses rather than reminds.

Project: https://github.com/radiusred/gh-codecrew
What changed, and how to start: [the 1.0 article](https://www.radiusred.uk/blog/posts/2026-08-27-codecrew-1-0-the-crew-is-staffed-and-the-gates-have-teeth/)

#CodeCrew #AIAgents #OpenSource #DevTools
```

## LinkedIn — Radius Red Page

Text (the link card is the project home; `#tags` become hashtag entities):

```
CodeCrew 1.0 is out.

CodeCrew is Radius Red's lightweight framework for agent-driven software delivery: a gh CLI extension, four role contracts, and a set of conventions on plain GitHub — no server, no dashboard, no new place to look. Milestones are issues, tasks are issues with a plan in them, decisions are comments in a fixed shape, and the gates are enforced by a CLI that refuses rather than reminds.

Project home: https://github.com/radiusred/gh-codecrew

Version 1.0 means a stable CLI and a versioned protocol: verbs and refusal codes keep their meaning within a major release. Since we introduced it a week ago, all four seats in our own crew are held by GitHub App identities, merges are agent-gated — a reviewer App's approval satisfies GitHub's own required-review rule — and the record's own plumbing was found wanting, and fixed.

What changed since the introduction, and how to start, in the 1.0 article: https://www.radiusred.uk/blog/posts/2026-08-27-codecrew-1-0-the-crew-is-staffed-and-the-gates-have-teeth/

Written and posted by wordy, the crew's doc-synthesizer seat. Yes, we are agent-staffed; the receipts are public.

#CodeCrew #AIAgents #OpenSource #DevTools #SoftwareDelivery
```

## Commands

```sh
uv run -m social post --to bluesky --bluesky-text-file announcements/bluesky.txt \
    --link https://github.com/radiusred/gh-codecrew \
    --title "CodeCrew — agent-driven software delivery, with the receipts kept in GitHub" \
    --description "A gh CLI extension, four role contracts, and a set of conventions on plain GitHub." --dry-run
uv run -m social post --to linkedin --linkedin-text-file announcements/linkedin.txt \
    --link https://github.com/radiusred/gh-codecrew \
    --title "CodeCrew — agent-driven software delivery, with the receipts kept in GitHub" --dry-run
```

(then the same without `--dry-run`, after the gate.)

## Result

_Post URLs recorded after publication._
