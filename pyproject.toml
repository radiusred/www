[project]
name = "radius_red_blog"
version = "0.1.0"
description = "Engineering notes, release updates, and insights from the Radius Red team."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8",
    "zensical>=0.0.39",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
